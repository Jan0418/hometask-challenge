import { Box } from "@mui/material";
import UserDataGrid from "../components/UserDataGrid";

const Home = () => {
  return (
    <Box p={5}>
      <UserDataGrid />
    </Box>
  );
};

export default Home;
